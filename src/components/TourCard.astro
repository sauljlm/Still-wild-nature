---
import Button from "./Button.astro";
const { tour, reverse = false } = Astro.props;
const { lang = "en" } = Astro.props;

const isEnglish = lang === "en";
const message = isEnglish
    ? `Hi, I would like to get more information about ${tour.title}`
    : `Hola, me gustaría saber más información sobre ${tour.title}`;

const contactUrl = `https://wa.me/50686654446?text=${encodeURIComponent(message)}`;
---

<article class="tour-card flex flex-col md:flex-row justify-between mb-20 md:mb-40">
  <div class={`image-anim parallax-wrapper w-full md:w-1/2 ${reverse ? "md:order-2" : ""}`}>
    <img
      src={tour.image}
      alt={tour.title}
      class="parallax-image"
    />
  </div>

  <div class={`text-anim w-full md:w-1/2 md:pl-10 md:pr-20 mt-6 md:mt-0 ${reverse ? "md:order-1" : ""}`}>
    <h3 class="text-[#4E7C59] text-4xl mb-1">{tour.title}</h3>
    <h4 class="text-xl mb-5">{tour.subtitle}</h4>

    <p>{tour.description}</p>

    <div class="mt-10">
      <h4 class="text-xl">Details</h4>

      <ul class="pb-10">
        {tour.details.map((detail) => (
          <li>
            <div class="flex items-center gap-2 py-1">
              <svg
                stroke="currentColor"
                fill="currentColor"
                viewBox="0 0 512 512"
                class="min-h-5 min-w-5 max-h-5 max-w-5 text-[#4E7C59]"
              >
                <path d="M256 8C119.033 8 8 119.033 8 256s111.033 248 248 248 248-111.033 248-248S392.967 8 256 8zm0 48c110.532 0 200 89.451 200 200 0 110.532-89.451 200-200 200-110.532 0-200-89.451-200-200 0-110.532 89.451-200 200-200m140.204 130.267l-22.536-22.718c-4.667-4.705-12.265-4.736-16.97-.068L215.346 303.697l-59.792-60.277c-4.667-4.705-12.265-4.736-16.97-.069l-22.719 22.536c-4.705 4.667-4.736 12.265-.068 16.971l90.781 91.516c4.667 4.705 12.265 4.736 16.97.068l172.589-171.204c4.704-4.668 4.734-12.266.067-16.971z" />
              </svg>
  
              <span class="text-sm md:text-[15px] font-medium">
                {detail}
              </span>
            </div>
          </li>
        ))}
      </ul>

      <Button variant="secondary">
        <a href={contactUrl} target="_blank">
          {isEnglish ? "Reserve" : "Reservar"}
        </a>
      </Button>
    </div>
  </div>

</article>

<style>
  .parallax-wrapper {
    position: relative;
    height: 400px;
    overflow: hidden;
  }

  @media (min-width: 1024px) {
    .parallax-wrapper {
      height: 500px;
    }
  }

  @media (min-width: 768px) {
    .parallax-wrapper {
      height: 600px;
    }
  }

  .parallax-image {
      position: absolute;
      width: 100%;
      height: 140%;
      object-fit: cover;
      top: -20%;
      left: 0;
      will-change: transform;
  }
</style>